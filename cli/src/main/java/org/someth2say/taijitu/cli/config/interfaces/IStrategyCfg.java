package org.someth2say.taijitu.cli.config.interfaces;


import org.someth2say.taijitu.cli.util.Named;

/**
 * Right now, strategies are only name references, so no configuration here.
 */
public interface IStrategyCfg extends ICfg, Named {
}
